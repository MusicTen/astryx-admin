# i18n 中英多语言实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 astryx-admin 添加中/英双语支持:react-i18next + JSON 资源,语言状态入 zustand ui store 持久化,顶栏/登录页/外观设置页三处切换入口,全部 UI 硬编码中文迁移为 `t()` 调用。

**Architecture:** i18next 以内联资源同步初始化(`src/i18n/`),`Language` 状态存于现有 ui store(persist key `astryx-admin-ui`),`AppProviders` 用 effect 同步 store → `i18n.changeLanguage` + `document.documentElement.lang`。文案按领域分层单 namespace。mock 业务数据不翻译。

**Tech Stack:** react-i18next、i18next、zustand(现有)、vitest(现有)。

**Spec:** `docs/superpowers/specs/2026-07-10-i18n-design.md`

## Global Constraints

- 每个 shell 命令前必须 `export PATH="$HOME/.nvm/versions/node/v22.16.0/bin:$PATH"`(默认 shell 是 Node 16,项目要求 Node ≥22.13)。
- 包管理用 pnpm;测试 `pnpm test`,构建 `pnpm build`,lint `pnpm lint`(内部是 vp/vite-plus)。
- 不写 `<div>`,布局用 Astryx 组件(Stack/Center 等);样式值用 token(`var(--color-*)` 等),不写裸 hex/px。
- 用 Astryx 组件前先 `pnpm exec astryx component <Name> --dense --detail compact` 查 props。
- commit message 用中文、conventional commits 格式(feat/fix/test/chore),不加 Co-Authored-By。
- 只迁移 UI 文案;代码注释里的中文、mock 数据(`src/mocks/`)里的中文一律不动。
- 翻译 key 引用格式统一 `t("a.b.c")`;插值用 i18next 语法 `{{name}}`。

---

### Task 1: i18n 基础设施(依赖、资源、初始化、检测、测试)

**Files:**
- Create: `src/i18n/index.ts`
- Create: `src/i18n/locales/zh.json`
- Create: `src/i18n/locales/en.json`
- Create: `src/i18n/index.test.ts`
- Create: `src/test/setup.ts`
- Modify: `vite.config.ts`(test 配置加 setupFiles)
- Modify: `package.json`(新依赖,通过 pnpm add)

**Interfaces:**
- Produces:
  - `type Language = "zh" | "en"`(从 `src/i18n/index.ts` 导出)
  - `detectLanguage(browserLanguage: string | undefined): Language`
  - `getInitialLanguage(): Language`(内部调 detectLanguage(navigator.language))
  - `initI18n(language: Language): void`(幂等,已初始化则跳过)
  - 全量 zh/en 翻译资源(后续任务只消费 key,不再新增 key;若发现遗漏 key 可补,但必须 zh/en 同时补)

- [ ] **Step 1: 安装依赖**

```bash
pnpm add i18next react-i18next
```

预期:package.json dependencies 出现 `i18next`、`react-i18next`。

- [ ] **Step 2: 写失败测试**

创建 `src/i18n/index.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import en from "./locales/en.json";
import zh from "./locales/zh.json";
import { detectLanguage } from "./index";

describe("detectLanguage", () => {
  it("zh 开头的浏览器语言返回 zh", () => {
    expect(detectLanguage("zh-CN")).toBe("zh");
    expect(detectLanguage("zh-TW")).toBe("zh");
    expect(detectLanguage("zh")).toBe("zh");
    expect(detectLanguage("ZH-CN")).toBe("zh");
  });

  it("非 zh 语言返回 en", () => {
    expect(detectLanguage("en-US")).toBe("en");
    expect(detectLanguage("ja")).toBe("en");
  });

  it("无浏览器语言时回退 zh", () => {
    expect(detectLanguage(undefined)).toBe("zh");
    expect(detectLanguage("")).toBe("zh");
  });
});

function flattenKeys(obj: Record<string, unknown>, prefix = ""): string[] {
  return Object.entries(obj).flatMap(([key, value]) => {
    const path = prefix ? `${prefix}.${key}` : key;
    return typeof value === "object" && value !== null
      ? flattenKeys(value as Record<string, unknown>, path)
      : [path];
  });
}

describe("翻译资源", () => {
  it("zh 与 en 的 key 集合完全一致", () => {
    expect(flattenKeys(en as Record<string, unknown>).sort()).toEqual(
      flattenKeys(zh as Record<string, unknown>).sort(),
    );
  });
});
```

- [ ] **Step 3: 运行测试确认失败**

```bash
pnpm test src/i18n
```

预期:FAIL(模块不存在)。

- [ ] **Step 4: 创建 zh.json**

创建 `src/i18n/locales/zh.json`(内容 = 现有 UI 全部中文原文):

```json
{
  "common": {
    "cancel": "取消",
    "delete": "删除",
    "edit": "编辑",
    "save": "保存",
    "create": "创建",
    "actionFailed": "操作失败，请稍后重试",
    "deleteFailed": "删除失败，请稍后重试"
  },
  "nav": {
    "sectionOverview": "概览",
    "sectionWork": "工作",
    "sectionManagement": "管理",
    "dashboard": "仪表盘",
    "tasks": "任务看板",
    "apps": "应用集成",
    "users": "用户管理",
    "settings": "系统设置",
    "profile": "个人资料",
    "appearance": "外观"
  },
  "userMenu": {
    "account": "账号",
    "profile": "个人资料",
    "appearanceSettings": "外观设置",
    "logout": "退出登录"
  },
  "theme": {
    "label": "主题模式",
    "light": "亮色",
    "dark": "暗色",
    "system": "系统"
  },
  "language": {
    "label": "语言"
  },
  "login": {
    "demoHint": "演示账号：任意用户名 / admin123",
    "username": "用户名",
    "password": "密码",
    "submit": "登录",
    "failed": "登录失败，请稍后重试"
  },
  "dashboard": {
    "title": "仪表盘",
    "tabs": {
      "overview": "概览",
      "analytics": "分析",
      "reports": "报表",
      "notifications": "通知"
    },
    "comingSoon": {
      "title": "暂未开放",
      "description": "该页签的内容尚未实现"
    },
    "stats": {
      "userTotal": "用户总数",
      "userTotalTrend": "较上月 +12.4%",
      "orderTotal": "订单总数",
      "orderTotalTrend": "较上月 +19%",
      "activeToday": "今日活跃",
      "activeTodayTrend": "较昨日 +8.2%",
      "errorCount": "今日告警",
      "errorCountTrend": "较昨日 -2 起"
    },
    "trends": {
      "title": "概览",
      "revenue": "营收"
    },
    "recentSales": {
      "title": "最近销售",
      "summary": "本月共成交 {{total}} 笔",
      "emptyTitle": "暂无销售记录",
      "emptyDescription": "产生新订单后会显示在这里"
    }
  },
  "tasks": {
    "title": "任务看板",
    "status": {
      "backlog": "待办",
      "inProgress": "进行中",
      "inReview": "评审中",
      "done": "已完成"
    },
    "priority": {
      "urgent": "紧急",
      "high": "高",
      "medium": "中",
      "low": "低"
    },
    "actionsLabel": "任务操作",
    "new": "新建任务",
    "updated": "任务已更新",
    "created": "任务已创建",
    "deleted": "任务已删除",
    "moveFailed": "移动任务失败，请稍后重试",
    "emptyTitle": "暂无任务",
    "emptyDescription": "点击右上角新建任务",
    "unassigned": "未分配",
    "deleteDialog": {
      "title": "删除任务",
      "description": "确定删除「{{title}}」吗？此操作不可撤销。"
    },
    "form": {
      "editTitle": "编辑任务",
      "createTitle": "新建任务",
      "title": "标题",
      "status": "状态",
      "priority": "优先级",
      "assignee": "负责人",
      "tags": "标签（逗号分隔）"
    }
  },
  "users": {
    "title": "用户列表",
    "subtitle": "在这里管理用户及其角色",
    "pageHeaderLabel": "用户页面标题",
    "role": {
      "admin": "管理员",
      "editor": "编辑",
      "viewer": "访客"
    },
    "status": {
      "active": "启用",
      "suspended": "停用",
      "invited": "已邀请"
    },
    "updated": "用户已更新",
    "created": "用户已创建",
    "deleted": "用户已删除",
    "batchDeleted": "已删除 {{count}} 个用户",
    "batchDeleteFailed": "批量删除失败，请稍后重试",
    "columns": {
      "username": "用户名",
      "name": "姓名",
      "email": "邮箱",
      "phone": "手机号码",
      "status": "状态",
      "role": "角色",
      "actions": "操作"
    },
    "actionsLabel": "用户操作",
    "invite": "邀请用户",
    "inviteComingSoon": "邀请功能开发中，敬请期待",
    "add": "添加用户",
    "filterLabel": "用户筛选",
    "search": "搜索",
    "searchPlaceholder": "按用户名/姓名/邮箱搜索",
    "batchDelete": "批量删除 ({{count}})",
    "emptyTitle": "暂无用户",
    "emptyDescription": "调整筛选条件，或点击右上角添加用户",
    "paginationLabel": "用户列表分页",
    "deleteDialog": {
      "title": "删除用户",
      "description": "确定删除「{{name}}」吗？此操作不可撤销。"
    },
    "batchDeleteDialog": {
      "title": "批量删除用户",
      "description": "确定删除选中的 {{count}} 个用户吗？此操作不可撤销。"
    },
    "form": {
      "editTitle": "编辑用户",
      "addTitle": "添加用户"
    }
  },
  "apps": {
    "title": "应用集成",
    "filterLabel": "按分类筛选",
    "categories": {
      "all": "全部",
      "collaboration": "沟通协作",
      "devtools": "开发工具",
      "payment": "支付",
      "automation": "自动化",
      "analytics": "数据分析"
    },
    "connected": "已连接",
    "disconnected": "已断开连接",
    "emptyTitle": "没有匹配的应用",
    "emptyDescription": "切换分类查看其他应用"
  },
  "settings": {
    "appearance": {
      "pageTitle": "外观设置",
      "cardTitle": "外观",
      "cardDescription": "调整界面的主题模式与语言"
    },
    "profile": {
      "pageTitle": "个人资料",
      "notLoggedIn": "未登录"
    }
  },
  "errors": {
    "requestFailed": "请求失败（HTTP {{status}}）"
  }
}
```

- [ ] **Step 5: 创建 en.json**

创建 `src/i18n/locales/en.json`(key 结构与 zh.json 完全一致):

```json
{
  "common": {
    "cancel": "Cancel",
    "delete": "Delete",
    "edit": "Edit",
    "save": "Save",
    "create": "Create",
    "actionFailed": "Action failed. Please try again later.",
    "deleteFailed": "Delete failed. Please try again later."
  },
  "nav": {
    "sectionOverview": "Overview",
    "sectionWork": "Work",
    "sectionManagement": "Management",
    "dashboard": "Dashboard",
    "tasks": "Task Board",
    "apps": "Integrations",
    "users": "Users",
    "settings": "Settings",
    "profile": "Profile",
    "appearance": "Appearance"
  },
  "userMenu": {
    "account": "Account",
    "profile": "Profile",
    "appearanceSettings": "Appearance",
    "logout": "Log Out"
  },
  "theme": {
    "label": "Theme mode",
    "light": "Light",
    "dark": "Dark",
    "system": "System"
  },
  "language": {
    "label": "Language"
  },
  "login": {
    "demoHint": "Demo account: any username / admin123",
    "username": "Username",
    "password": "Password",
    "submit": "Log In",
    "failed": "Login failed. Please try again later."
  },
  "dashboard": {
    "title": "Dashboard",
    "tabs": {
      "overview": "Overview",
      "analytics": "Analytics",
      "reports": "Reports",
      "notifications": "Notifications"
    },
    "comingSoon": {
      "title": "Coming Soon",
      "description": "This tab has not been implemented yet"
    },
    "stats": {
      "userTotal": "Total Users",
      "userTotalTrend": "+12.4% vs last month",
      "orderTotal": "Total Orders",
      "orderTotalTrend": "+19% vs last month",
      "activeToday": "Active Today",
      "activeTodayTrend": "+8.2% vs yesterday",
      "errorCount": "Alerts Today",
      "errorCountTrend": "2 fewer vs yesterday"
    },
    "trends": {
      "title": "Overview",
      "revenue": "Revenue"
    },
    "recentSales": {
      "title": "Recent Sales",
      "summary": "{{total}} sales closed this month",
      "emptyTitle": "No sales yet",
      "emptyDescription": "New orders will show up here"
    }
  },
  "tasks": {
    "title": "Task Board",
    "status": {
      "backlog": "Backlog",
      "inProgress": "In Progress",
      "inReview": "In Review",
      "done": "Done"
    },
    "priority": {
      "urgent": "Urgent",
      "high": "High",
      "medium": "Medium",
      "low": "Low"
    },
    "actionsLabel": "Task actions",
    "new": "New Task",
    "updated": "Task updated",
    "created": "Task created",
    "deleted": "Task deleted",
    "moveFailed": "Failed to move task. Please try again later.",
    "emptyTitle": "No tasks",
    "emptyDescription": "Click \"New Task\" in the top right to create one",
    "unassigned": "Unassigned",
    "deleteDialog": {
      "title": "Delete Task",
      "description": "Delete \"{{title}}\"? This action cannot be undone."
    },
    "form": {
      "editTitle": "Edit Task",
      "createTitle": "New Task",
      "title": "Title",
      "status": "Status",
      "priority": "Priority",
      "assignee": "Assignee",
      "tags": "Tags (comma separated)"
    }
  },
  "users": {
    "title": "Users",
    "subtitle": "Manage users and their roles here",
    "pageHeaderLabel": "Users page header",
    "role": {
      "admin": "Admin",
      "editor": "Editor",
      "viewer": "Viewer"
    },
    "status": {
      "active": "Active",
      "suspended": "Suspended",
      "invited": "Invited"
    },
    "updated": "User updated",
    "created": "User created",
    "deleted": "User deleted",
    "batchDeleted": "Deleted {{count}} users",
    "batchDeleteFailed": "Batch delete failed. Please try again later.",
    "columns": {
      "username": "Username",
      "name": "Name",
      "email": "Email",
      "phone": "Phone",
      "status": "Status",
      "role": "Role",
      "actions": "Actions"
    },
    "actionsLabel": "User actions",
    "invite": "Invite User",
    "inviteComingSoon": "Invitations are coming soon",
    "add": "Add User",
    "filterLabel": "User filters",
    "search": "Search",
    "searchPlaceholder": "Search by username, name, or email",
    "batchDelete": "Batch Delete ({{count}})",
    "emptyTitle": "No users",
    "emptyDescription": "Adjust the filters, or click \"Add User\" in the top right",
    "paginationLabel": "User list pagination",
    "deleteDialog": {
      "title": "Delete User",
      "description": "Delete \"{{name}}\"? This action cannot be undone."
    },
    "batchDeleteDialog": {
      "title": "Batch Delete Users",
      "description": "Delete the {{count}} selected users? This action cannot be undone."
    },
    "form": {
      "editTitle": "Edit User",
      "addTitle": "Add User"
    }
  },
  "apps": {
    "title": "Integrations",
    "filterLabel": "Filter by category",
    "categories": {
      "all": "All",
      "collaboration": "Collaboration",
      "devtools": "Developer Tools",
      "payment": "Payments",
      "automation": "Automation",
      "analytics": "Analytics"
    },
    "connected": "Connected",
    "disconnected": "Disconnected",
    "emptyTitle": "No matching apps",
    "emptyDescription": "Switch categories to see other apps"
  },
  "settings": {
    "appearance": {
      "pageTitle": "Appearance",
      "cardTitle": "Appearance",
      "cardDescription": "Adjust the interface theme and language"
    },
    "profile": {
      "pageTitle": "Profile",
      "notLoggedIn": "Not logged in"
    }
  },
  "errors": {
    "requestFailed": "Request failed (HTTP {{status}})"
  }
}
```

- [ ] **Step 6: 创建 `src/i18n/index.ts`**

```ts
import i18n from "i18next";
import { initReactI18next } from "react-i18next";
import en from "./locales/en.json";
import zh from "./locales/zh.json";

export type Language = "zh" | "en";

export function detectLanguage(browserLanguage: string | undefined): Language {
  if (!browserLanguage) return "zh";
  return browserLanguage.toLowerCase().startsWith("zh") ? "zh" : "en";
}

export function getInitialLanguage(): Language {
  return detectLanguage(typeof navigator === "undefined" ? undefined : navigator.language);
}

export function initI18n(language: Language): void {
  if (i18n.isInitialized) return;
  void i18n.use(initReactI18next).init({
    resources: {
      zh: { translation: zh },
      en: { translation: en },
    },
    lng: language,
    fallbackLng: "zh",
    interpolation: { escapeValue: false },
  });
}
```

注意:tsconfig 若未开 `resolveJsonModule`,需在 `tsconfig.json`(或 `tsconfig.app.json`)的 compilerOptions 中加 `"resolveJsonModule": true`。

- [ ] **Step 7: 创建测试 setup 并接入 vitest**

创建 `src/test/setup.ts`:

```ts
import { initI18n } from "../i18n";

// 测试环境固定中文,现有断言中文文案的测试保持不变
initI18n("zh");
```

修改 `vite.config.ts` 的 test 字段:

```ts
  test: {
    environment: "happy-dom",
    setupFiles: ["./src/test/setup.ts"],
  },
```

- [ ] **Step 8: 运行测试确认通过**

```bash
pnpm test src/i18n
```

预期:PASS(detectLanguage 6 断言 + key 一致性 1 条)。再跑全量 `pnpm test` 确认无回归。

- [ ] **Step 9: Commit**

```bash
git add src/i18n src/test vite.config.ts package.json pnpm-lock.yaml tsconfig*.json
git commit -m "feat: 引入 i18next 基础设施与中英文案资源"
```

---

### Task 2: ui store 语言状态 + AppProviders 同步

**Files:**
- Modify: `src/stores/ui.ts`
- Modify: `src/stores/ui.test.ts`
- Modify: `src/app/providers.tsx`
- Modify: `src/main.tsx`

**Interfaces:**
- Consumes: `Language`、`getInitialLanguage`、`initI18n`(Task 1,来自 `src/i18n`)
- Produces: `useUiStore` 新增 `language: Language` 与 `setLanguage(language: Language): void`,持久化于现有 `astryx-admin-ui` key。后续任务(LanguageControl)依赖这两个成员。

- [ ] **Step 1: 写失败测试**

在 `src/stores/ui.test.ts` 追加:

```ts
test("语言可更新", () => {
  useUiStore.getState().setLanguage("en");
  expect(useUiStore.getState().language).toBe("en");
  useUiStore.getState().setLanguage("zh");
  expect(useUiStore.getState().language).toBe("zh");
});
```

- [ ] **Step 2: 运行确认失败**

```bash
pnpm test src/stores/ui
```

预期:FAIL(`setLanguage` 不存在)。

- [ ] **Step 3: 实现 store 字段**

`src/stores/ui.ts` 改为:

```ts
import { create } from "zustand";
import { persist } from "zustand/middleware";
import { immer } from "zustand/middleware/immer";
import { getInitialLanguage, type Language } from "../i18n";

export type ThemeMode = "light" | "dark" | "system";
export type { Language };

interface UiState {
  themeMode: ThemeMode;
  isSideNavCollapsed: boolean;
  language: Language;
  setThemeMode: (mode: ThemeMode) => void;
  setSideNavCollapsed: (isCollapsed: boolean) => void;
  setLanguage: (language: Language) => void;
}

export const useUiStore = create<UiState>()(
  persist(
    immer((set) => ({
      themeMode: "system",
      isSideNavCollapsed: false,
      language: getInitialLanguage(),
      setThemeMode: (mode) =>
        set((state) => {
          state.themeMode = mode;
        }),
      setSideNavCollapsed: (isCollapsed) =>
        set((state) => {
          state.isSideNavCollapsed = isCollapsed;
        }),
      setLanguage: (language) =>
        set((state) => {
          state.language = language;
        }),
    })),
    {
      name: "astryx-admin-ui",
      merge: (persisted, current) => {
        // localStorage 里的非法 language 值(如手改或旧版本)回退到浏览器检测值
        const merged = { ...current, ...(persisted as Partial<UiState>) };
        if (merged.language !== "zh" && merged.language !== "en") {
          merged.language = getInitialLanguage();
        }
        return merged;
      },
    },
  ),
);
```

- [ ] **Step 4: main.tsx 初始化 i18n**

`src/main.tsx` 在 createRouter 之前加(import 区域):

```ts
import { initI18n } from "./i18n";
import { useUiStore } from "./stores/ui";

initI18n(useUiStore.getState().language);
```

(store persist 同步 rehydrate,`getState().language` 即持久化值或浏览器检测值。)

- [ ] **Step 5: AppProviders 同步语言**

`src/app/providers.tsx` 中:

```tsx
import { useEffect, forwardRef, type ComponentPropsWithoutRef, type ReactNode } from "react";
import i18n from "i18next";
```

`AppProviders` 函数体加:

```tsx
export function AppProviders({ children }: { children: ReactNode }) {
  const themeMode = useUiStore((state) => state.themeMode);
  const language = useUiStore((state) => state.language);

  useEffect(() => {
    void i18n.changeLanguage(language);
    document.documentElement.lang = language === "zh" ? "zh-CN" : "en";
  }, [language]);

  return (
    <Theme theme={appTheme} mode={themeMode}>
      <LinkProvider component={AppLink}>
        <SwrProvider>{children}</SwrProvider>
      </LinkProvider>
    </Theme>
  );
}
```

注意:先确认 `AppProviders` 是否包住登录页——查 `src/routes/__root.tsx`;若 AppProviders 只包 `_auth` 布局,把语言同步 effect 放到 `__root.tsx` 层级(或将 AppProviders 上移),保证登录页切语言也生效。

- [ ] **Step 6: 运行测试 + 手动验证**

```bash
pnpm test
```

预期:全部 PASS。

- [ ] **Step 7: Commit**

```bash
git add src/stores/ui.ts src/stores/ui.test.ts src/app/providers.tsx src/main.tsx src/routes/__root.tsx
git commit -m "feat: ui store 增加语言状态并在启动与运行时同步 i18n"
```

---

### Task 3: LanguageControl 组件与三处放置

**Files:**
- Create: `src/components/layout/LanguageControl.tsx`
- Modify: `src/components/layout/AdminShell.tsx`(顶栏 endContent)
- Modify: `src/routes/login.tsx`
- Modify: `src/features/settings/AppearanceCard.tsx`

**Interfaces:**
- Consumes: `useUiStore` 的 `language`/`setLanguage`(Task 2)、`t("language.label")`/`t("theme.label")`/`t("settings.appearance.*")`(Task 1)
- Produces: `LanguageControl` 组件(无 props),供任意位置放置。

- [ ] **Step 1: 创建 LanguageControl**

`src/components/layout/LanguageControl.tsx`(仿 ThemeModeControl 模式;语言名固定以自身语言显示,不翻译):

```tsx
import { SegmentedControl, SegmentedControlItem } from "@astryxdesign/core/SegmentedControl";
import { useTranslation } from "react-i18next";
import { useUiStore, type Language } from "../../stores/ui";

const LANGUAGES: { value: Language; label: string }[] = [
  { value: "zh", label: "中文" },
  { value: "en", label: "English" },
];

export function LanguageControl() {
  const { t } = useTranslation();
  const language = useUiStore((state) => state.language);
  const setLanguage = useUiStore((state) => state.setLanguage);
  return (
    <SegmentedControl
      label={t("language.label")}
      size="sm"
      value={language}
      onChange={(value) => setLanguage(value as Language)}
    >
      {LANGUAGES.map((item) => (
        <SegmentedControlItem key={item.value} value={item.value} label={item.label} />
      ))}
    </SegmentedControl>
  );
}
```

- [ ] **Step 2: 顶栏放置**

`src/components/layout/AdminShell.tsx` 的 endContent Stack 中,在 `<ThemeModeControl />` 前加 `<LanguageControl />`:

```tsx
endContent={
  <Stack direction="horizontal" gap={3}>
    <LanguageControl />
    <ThemeModeControl />
    <UserMenu />
  </Stack>
}
```

- [ ] **Step 3: 登录页放置**

`src/routes/login.tsx` 的 LoginPage:

```tsx
import { Stack } from "@astryxdesign/core/Stack";
import { Center } from "@astryxdesign/core/Center";
import { LanguageControl } from "../components/layout/LanguageControl";

function LoginPage() {
  const navigate = useNavigate();
  const { redirect } = Route.useSearch();
  return (
    <Center axis="both" minHeight="100dvh" className="login-page-background">
      <Stack direction="vertical" gap={4}>
        <LoginForm onSuccess={() => void navigate({ to: redirect ?? "/" })} />
        <Center axis="horizontal">
          <LanguageControl />
        </Center>
      </Stack>
    </Center>
  );
}
```

- [ ] **Step 4: 外观设置卡放置(同时迁移该卡文案)**

`src/features/settings/AppearanceCard.tsx` 改为:

```tsx
import { Card } from "@astryxdesign/core/Card";
import { Stack } from "@astryxdesign/core/Stack";
import { Text } from "@astryxdesign/core/Text";
import { useTranslation } from "react-i18next";
import { LanguageControl } from "../../components/layout/LanguageControl";
import { ThemeModeControl } from "../../components/layout/ThemeModeControl";

export function AppearanceCard() {
  const { t } = useTranslation();
  return (
    <Card padding={6} width={420}>
      <Stack direction="vertical" gap={4}>
        <Stack direction="vertical" gap={1}>
          <Text type="large">{t("settings.appearance.cardTitle")}</Text>
          <Text type="supporting" color="secondary">
            {t("settings.appearance.cardDescription")}
          </Text>
        </Stack>
        <Stack direction="vertical" gap={2}>
          <Text type="supporting">{t("theme.label")}</Text>
          <ThemeModeControl />
        </Stack>
        <Stack direction="vertical" gap={2}>
          <Text type="supporting">{t("language.label")}</Text>
          <LanguageControl />
        </Stack>
      </Stack>
    </Card>
  );
}
```

- [ ] **Step 5: 验证**

```bash
pnpm test && pnpm lint
```

预期:PASS。可选:`pnpm dev` 手动确认顶栏/登录页/外观页三处切换生效且持久化(刷新后保持)。

- [ ] **Step 6: Commit**

```bash
git add src/components/layout/LanguageControl.tsx src/components/layout/AdminShell.tsx src/routes/login.tsx src/features/settings/AppearanceCard.tsx
git commit -m "feat: 新增语言切换控件并接入顶栏、登录页与外观设置"
```

---

### Task 4: 布局层文案迁移(AdminShell / PageBreadcrumbs / UserMenu / ThemeModeControl)

**Files:**
- Modify: `src/components/layout/AdminShell.tsx`
- Modify: `src/components/layout/PageBreadcrumbs.tsx`
- Modify: `src/components/layout/PageBreadcrumbs.test.ts`
- Modify: `src/components/layout/UserMenu.tsx`
- Modify: `src/components/layout/ThemeModeControl.tsx`

**Interfaces:**
- Consumes: Task 1 的 `nav.*`、`userMenu.*`、`theme.*` key
- Produces: `getBreadcrumbLabels(pathname: string): string[]` 语义变更 —— 返回 i18n key 数组而非中文,翻译在组件渲染层完成。

- [ ] **Step 1: 更新 PageBreadcrumbs 测试(先改测试)**

`src/components/layout/PageBreadcrumbs.test.ts` 断言改为 key:

```ts
import { describe, expect, it } from "vitest";
import { getBreadcrumbLabels } from "./PageBreadcrumbs";

describe("getBreadcrumbLabels", () => {
  it("returns an empty array for the dashboard root route", () => {
    expect(getBreadcrumbLabels("/")).toEqual([]);
  });

  it("returns a single label key for a top-level route", () => {
    expect(getBreadcrumbLabels("/users")).toEqual(["nav.users"]);
  });

  it("returns a two-level trail for nested settings routes", () => {
    expect(getBreadcrumbLabels("/settings/appearance")).toEqual(["nav.settings", "nav.appearance"]);
    expect(getBreadcrumbLabels("/settings/profile")).toEqual(["nav.settings", "nav.profile"]);
  });

  it("falls back to an empty array for unknown routes", () => {
    expect(getBreadcrumbLabels("/unknown")).toEqual([]);
  });
});
```

- [ ] **Step 2: 运行确认失败**

```bash
pnpm test src/components/layout/PageBreadcrumbs
```

预期:FAIL(仍返回中文)。

- [ ] **Step 3: 实现 PageBreadcrumbs**

```tsx
import { Breadcrumbs, BreadcrumbItem } from "@astryxdesign/core/Breadcrumbs";
import { useTranslation } from "react-i18next";

const BREADCRUMB_KEYS: Record<string, string[]> = {
  "/users": ["nav.users"],
  "/settings/profile": ["nav.settings", "nav.profile"],
  "/settings/appearance": ["nav.settings", "nav.appearance"],
};

export function getBreadcrumbLabels(pathname: string): string[] {
  return BREADCRUMB_KEYS[pathname] ?? [];
}

export function PageBreadcrumbs({ pathname }: { pathname: string }) {
  const { t } = useTranslation();
  const keys = getBreadcrumbLabels(pathname);
  if (keys.length === 0) {
    return null;
  }

  return (
    <Breadcrumbs variant="supporting">
      {keys.map((key, index) => (
        <BreadcrumbItem key={key} isCurrent={index === keys.length - 1}>
          {t(key)}
        </BreadcrumbItem>
      ))}
    </Breadcrumbs>
  );
}
```

- [ ] **Step 4: AdminShell 侧边栏迁移**

`AdminShell` 函数内加 `const { t } = useTranslation();`(import `useTranslation` from "react-i18next"),替换:

| 原文 | 替换为 |
| --- | --- |
| `title="概览"` | `title={t("nav.sectionOverview")}` |
| `label="仪表盘"` | `label={t("nav.dashboard")}` |
| `title="工作"` | `title={t("nav.sectionWork")}` |
| `label="任务看板"` | `label={t("nav.tasks")}` |
| `label="应用集成"` | `label={t("nav.apps")}` |
| `title="管理"` | `title={t("nav.sectionManagement")}` |
| `label="用户管理"` | `label={t("nav.users")}` |
| `label="系统设置"` | `label={t("nav.settings")}` |
| `label="个人资料"` | `label={t("nav.profile")}` |
| `label="外观"` | `label={t("nav.appearance")}` |

- [ ] **Step 5: UserMenu 迁移**

函数内加 `const { t } = useTranslation();`,替换:

| 原文 | 替换为 |
| --- | --- |
| `user?.name ?? "账号"`(两处) | `user?.name ?? t("userMenu.account")` |
| `label="个人资料"` | `label={t("userMenu.profile")}` |
| `label="外观设置"` | `label={t("userMenu.appearanceSettings")}` |
| `退出登录`(Text 内) | `{t("userMenu.logout")}` |

- [ ] **Step 6: ThemeModeControl 迁移**

模块级 MODES 数组改存 key,渲染时翻译:

```tsx
import { SegmentedControl, SegmentedControlItem } from "@astryxdesign/core/SegmentedControl";
import { useTranslation } from "react-i18next";
import { useUiStore, type ThemeMode } from "../../stores/ui";

const MODES: { value: ThemeMode; labelKey: string }[] = [
  { value: "light", labelKey: "theme.light" },
  { value: "dark", labelKey: "theme.dark" },
  { value: "system", labelKey: "theme.system" },
];

export function ThemeModeControl() {
  const { t } = useTranslation();
  const themeMode = useUiStore((state) => state.themeMode);
  const setThemeMode = useUiStore((state) => state.setThemeMode);
  return (
    <SegmentedControl
      label={t("theme.label")}
      size="sm"
      value={themeMode}
      onChange={(value) => setThemeMode(value as ThemeMode)}
    >
      {MODES.map((mode) => (
        <SegmentedControlItem key={mode.value} value={mode.value} label={t(mode.labelKey)} />
      ))}
    </SegmentedControl>
  );
}
```

- [ ] **Step 7: 验证 + Commit**

```bash
pnpm test && pnpm lint
git add src/components/layout
git commit -m "feat: 布局层(侧边栏/面包屑/用户菜单/主题切换)文案接入 i18n"
```

---

### Task 5: 登录页文案迁移(LoginForm)

**Files:**
- Modify: `src/features/auth/LoginForm.tsx`

**Interfaces:**
- Consumes: Task 1 的 `login.*` key

- [ ] **Step 1: 迁移 LoginForm**

函数内加 `const { t } = useTranslation();`(import from "react-i18next"),替换:

| 原文 | 替换为 |
| --- | --- |
| `"登录失败，请稍后重试"` | `t("login.failed")` |
| `演示账号：任意用户名 / admin123` | `{t("login.demoHint")}` |
| `label="用户名"` | `label={t("login.username")}` |
| `label="密码"` | `label={t("login.password")}` |
| `label="登录"` | `label={t("login.submit")}` |

标题 `Astryx Admin` 是品牌名,不翻译。

- [ ] **Step 2: 验证 + Commit**

```bash
pnpm test && pnpm lint
git add src/features/auth/LoginForm.tsx
git commit -m "feat: 登录表单文案接入 i18n"
```

---

### Task 6: 仪表盘文案迁移

**Files:**
- Modify: `src/routes/_auth/index.tsx`
- Modify: `src/features/dashboard/StatCards.tsx`
- Modify: `src/features/dashboard/TrendCharts.tsx`
- Modify: `src/features/dashboard/RecentSalesList.tsx`

**Interfaces:**
- Consumes: Task 1 的 `dashboard.*` key

- [ ] **Step 1: 迁移 dashboard 路由页**

`src/routes/_auth/index.tsx`:TABS 数组改存 key,组件内翻译:

```tsx
const TABS = [
  { value: "overview", labelKey: "dashboard.tabs.overview" },
  { value: "analytics", labelKey: "dashboard.tabs.analytics" },
  { value: "reports", labelKey: "dashboard.tabs.reports" },
  { value: "notifications", labelKey: "dashboard.tabs.notifications" },
];
```

组件内 `const { t } = useTranslation();`,替换:

| 原文 | 替换为 |
| --- | --- |
| `<Text type="display-3">仪表盘</Text>` | `<Text type="display-3">{t("dashboard.title")}</Text>` |
| `label={item.label}` | `label={t(item.labelKey)}` |
| `title="暂未开放" description="该页签的内容尚未实现"` | `title={t("dashboard.comingSoon.title")} description={t("dashboard.comingSoon.description")}` |

- [ ] **Step 2: 迁移 StatCards**

模块级 STATS 数组的 `title`/`trend` 改为 `titleKey`/`trendKey`:

```tsx
{ key: "userTotal", titleKey: "dashboard.stats.userTotal", icon: Users, trendKey: "dashboard.stats.userTotalTrend" },
{ key: "orderTotal", titleKey: "dashboard.stats.orderTotal", icon: CreditCard, trendKey: "dashboard.stats.orderTotalTrend" },
{ key: "activeToday", titleKey: "dashboard.stats.activeToday", icon: Activity, trendKey: "dashboard.stats.activeTodayTrend" },
{ key: "errorCount", titleKey: "dashboard.stats.errorCount", icon: TriangleAlert, trendKey: "dashboard.stats.errorCountTrend" },
```

组件内 `const { t } = useTranslation();`,渲染处 `t(stat.titleKey)` / `t(stat.trendKey)`(按现有渲染代码对应替换)。

- [ ] **Step 3: 迁移 TrendCharts**

| 原文 | 替换为 |
| --- | --- |
| `<Text type="large">概览</Text>` | `<Text type="large">{t("dashboard.trends.title")}</Text>` |
| `name="营收"` | `name={t("dashboard.trends.revenue")}` |

- [ ] **Step 4: 迁移 RecentSalesList**

| 原文 | 替换为 |
| --- | --- |
| `<Text type="large">最近销售</Text>` | `{t("dashboard.recentSales.title")}` |
| `本月共成交 {total} 笔` | `{t("dashboard.recentSales.summary", { total })}` |
| `title="暂无销售记录" description="产生新订单后会显示在这里"` | `title={t("dashboard.recentSales.emptyTitle")} description={t("dashboard.recentSales.emptyDescription")}` |

- [ ] **Step 5: 验证 + Commit**

```bash
pnpm test && pnpm lint
git add src/routes/_auth/index.tsx src/features/dashboard
git commit -m "feat: 仪表盘文案接入 i18n"
```

---

### Task 7: 任务看板文案迁移

**Files:**
- Modify: `src/routes/_auth/tasks.tsx`
- Modify: `src/features/tasks/TaskBoard.tsx`
- Modify: `src/features/tasks/TaskCard.tsx`
- Modify: `src/features/tasks/TaskFormDialog.tsx`

**Interfaces:**
- Consumes: Task 1 的 `tasks.*`、`common.*` key
- 注意:`src/features/tasks/groupTasksByStatus.ts` 及其测试是纯数据逻辑,不涉及文案,不改。

- [ ] **Step 1: 迁移 tasks 路由页**

`src/routes/_auth/tasks.tsx`:`<Text type="display-3">任务看板</Text>` → `{t("tasks.title")}`(组件内加 useTranslation)。

- [ ] **Step 2: 迁移 TaskBoard**

模块级状态列标题 map 改存 key:

```tsx
const STATUS_LABEL_KEYS: Record<TaskStatus, string> = {
  backlog: "tasks.status.backlog",
  "in-progress": "tasks.status.inProgress",
  "in-review": "tasks.status.inReview",
  done: "tasks.status.done",
};
```

(原变量名以实际文件为准,保持同名仅改值;渲染列标题处用 `t(STATUS_LABEL_KEYS[status])`。)

组件内替换:

| 原文 | 替换为 |
| --- | --- |
| `toast({ body: "任务已更新" })` | `toast({ body: t("tasks.updated") })` |
| `toast({ body: "任务已创建" })` | `toast({ body: t("tasks.created") })` |
| `notifyError(error, "操作失败，请稍后重试")` | `notifyError(error, t("common.actionFailed"))` |
| `toast({ body: "任务已删除" })` | `toast({ body: t("tasks.deleted") })` |
| `notifyError(error, "删除失败，请稍后重试")` | `notifyError(error, t("common.deleteFailed"))` |
| `notifyError(error, "移动任务失败，请稍后重试")` | `notifyError(error, t("tasks.moveFailed"))` |
| `label="任务操作"` | `label={t("tasks.actionsLabel")}` |
| `label="新建任务"` | `label={t("tasks.new")}` |
| `title="暂无任务" description="点击右上角新建任务"` | `title={t("tasks.emptyTitle")} description={t("tasks.emptyDescription")}` |
| `?? "未分配"` | `?? t("tasks.unassigned")` |
| `title="删除任务"` | `title={t("tasks.deleteDialog.title")}` |
| `` description={`确定删除「${deletingTask?.title ?? ""}」吗？此操作不可撤销。`} `` | `description={t("tasks.deleteDialog.description", { title: deletingTask?.title ?? "" })}` |
| `actionLabel="删除"` | `actionLabel={t("common.delete")}` |
| `cancelLabel="取消"` | `cancelLabel={t("common.cancel")}` |

- [ ] **Step 3: 迁移 TaskCard**

优先级 map 改存 key(保持原 variant):

```tsx
const PRIORITY_CONFIG: Record<TaskPriority, { labelKey: string; variant: BadgeVariant }> = {
  urgent: { labelKey: "tasks.priority.urgent", variant: "error" },
  high: { labelKey: "tasks.priority.high", variant: "warning" },
  medium: { labelKey: "tasks.priority.medium", variant: "teal" },
  low: { labelKey: "tasks.priority.low", variant: "neutral" },
};
```

(类型名/variant 类型以实际文件为准。)组件内 `t(config.labelKey)` 渲染;并替换:

| 原文 | 替换为 |
| --- | --- |
| `label="任务操作"` | `label={t("tasks.actionsLabel")}` |
| `{ label: "编辑", onClick: onEdit }` | `{ label: t("common.edit"), onClick: onEdit }` |
| `{ label: "删除", onClick: onDelete }` | `{ label: t("common.delete"), onClick: onDelete }` |

- [ ] **Step 4: 迁移 TaskFormDialog**

模块级选项数组改存 key:

```tsx
const STATUS_OPTIONS = [
  { labelKey: "tasks.status.backlog", value: "backlog" },
  { labelKey: "tasks.status.inProgress", value: "in-progress" },
  { labelKey: "tasks.status.inReview", value: "in-review" },
  { labelKey: "tasks.status.done", value: "done" },
];

const PRIORITY_OPTIONS = [
  { labelKey: "tasks.priority.low", value: "low" },
  { labelKey: "tasks.priority.medium", value: "medium" },
  { labelKey: "tasks.priority.high", value: "high" },
  { labelKey: "tasks.priority.urgent", value: "urgent" },
];
```

组件内构造带翻译 label 的选项传给 Select(如 `STATUS_OPTIONS.map((o) => ({ label: t(o.labelKey), value: o.value }))`),并替换:

| 原文 | 替换为 |
| --- | --- |
| `title={editingTask ? "编辑任务" : "新建任务"}` | `title={editingTask ? t("tasks.form.editTitle") : t("tasks.form.createTitle")}` |
| `label="标题"` | `label={t("tasks.form.title")}` |
| `label="状态"` | `label={t("tasks.form.status")}` |
| `label="优先级"` | `label={t("tasks.form.priority")}` |
| `label="负责人"` | `label={t("tasks.form.assignee")}` |
| `label="标签（逗号分隔）"` | `label={t("tasks.form.tags")}` |
| `label="取消"` | `label={t("common.cancel")}` |
| `label={editingTask ? "保存" : "创建"}` | `label={editingTask ? t("common.save") : t("common.create")}` |

- [ ] **Step 5: 验证 + Commit**

```bash
pnpm test && pnpm lint
git add src/routes/_auth/tasks.tsx src/features/tasks
git commit -m "feat: 任务看板文案接入 i18n"
```

---

### Task 8: 用户管理文案迁移

**Files:**
- Modify: `src/features/users/UserTable.tsx`
- Modify: `src/features/users/UserFormDialog.tsx`

**Interfaces:**
- Consumes: Task 1 的 `users.*`、`common.*` key

- [ ] **Step 1: 迁移 UserTable**

模块级角色/状态 map 改存 key:

```tsx
const ROLE_LABEL_KEYS: Record<UserRole, string> = {
  admin: "users.role.admin",
  editor: "users.role.editor",
  viewer: "users.role.viewer",
};

const STATUS_LABEL_KEYS: Record<UserStatus, string> = {
  active: "users.status.active",
  suspended: "users.status.suspended",
  invited: "users.status.invited",
};
```

(原 map 变量名与类型名以实际文件为准,值改为 key,所有读取处包 `t(...)`;筛选下拉的选项 label 同样由这两个 map 派生翻译。)

组件内替换(`const { t } = useTranslation();`;列定义如在组件外,移入组件内或改用 useMemo 依赖 t):

| 原文 | 替换为 |
| --- | --- |
| `toast({ body: "用户已更新" })` | `toast({ body: t("users.updated") })` |
| `toast({ body: "用户已创建" })` | `toast({ body: t("users.created") })` |
| `notifyError(error, "操作失败，请稍后重试")` | `notifyError(error, t("common.actionFailed"))` |
| `toast({ body: "用户已删除" })` | `toast({ body: t("users.deleted") })` |
| `notifyError(error, "删除失败，请稍后重试")` | `notifyError(error, t("common.deleteFailed"))` |
| `` toast({ body: `已删除 ${selectedKeys.size} 个用户` }) `` | `toast({ body: t("users.batchDeleted", { count: selectedKeys.size }) })` |
| `notifyError(error, "批量删除失败，请稍后重试")` | `notifyError(error, t("users.batchDeleteFailed"))` |
| `header: "用户名"` | `header: t("users.columns.username")` |
| `header: "姓名"` | `header: t("users.columns.name")` |
| `header: "邮箱"` | `header: t("users.columns.email")` |
| `header: "手机号码"` | `header: t("users.columns.phone")` |
| `header: "状态"` | `header: t("users.columns.status")` |
| `header: "角色"` | `header: t("users.columns.role")` |
| `header: "操作"` | `header: t("users.columns.actions")` |
| `label="用户操作"` | `label={t("users.actionsLabel")}` |
| `label: "编辑"` | `label: t("common.edit")` |
| `label: "删除"` | `label: t("common.delete")` |
| `label="用户页面标题"` | `label={t("users.pageHeaderLabel")}` |
| `<Text type="display-3">用户列表</Text>` | `{t("users.title")}` |
| `在这里管理用户及其角色` | `{t("users.subtitle")}` |
| `label="邀请用户"` | `label={t("users.invite")}` |
| `toast({ body: "邀请功能开发中，敬请期待" })` | `toast({ body: t("users.inviteComingSoon") })` |
| `label="添加用户"` | `label={t("users.add")}` |
| `label="用户筛选"` | `label={t("users.filterLabel")}` |
| `label="搜索"` | `label={t("users.search")}` |
| `placeholder="按用户名/姓名/邮箱搜索"` | `placeholder={t("users.searchPlaceholder")}` |
| `label="状态"` / `placeholder="状态"` | `t("users.columns.status")` |
| `label="角色"` / `placeholder="角色"` | `t("users.columns.role")` |
| `` label={`批量删除 (${selectedKeys.size})`} `` | `label={t("users.batchDelete", { count: selectedKeys.size })}` |
| `title="暂无用户" description="调整筛选条件，或点击右上角添加用户"` | `title={t("users.emptyTitle")} description={t("users.emptyDescription")}` |
| `label="用户列表分页"` | `label={t("users.paginationLabel")}` |
| `title="删除用户"` | `title={t("users.deleteDialog.title")}` |
| `` description={`确定删除「${deletingUser?.name ?? ""}」吗？…`} `` | `description={t("users.deleteDialog.description", { name: deletingUser?.name ?? "" })}` |
| `title="批量删除用户"` | `title={t("users.batchDeleteDialog.title")}` |
| `` description={`确定删除选中的 ${selectedKeys.size} 个用户吗？…`} `` | `description={t("users.batchDeleteDialog.description", { count: selectedKeys.size })}` |
| `actionLabel="删除"`(两处) | `actionLabel={t("common.delete")}` |
| `cancelLabel="取消"`(两处) | `cancelLabel={t("common.cancel")}` |

- [ ] **Step 2: 迁移 UserFormDialog**

模块级选项数组改存 key(同 Task 7 Step 4 的模式):

```tsx
const ROLE_OPTIONS = [
  { labelKey: "users.role.admin", value: "admin" },
  { labelKey: "users.role.editor", value: "editor" },
  { labelKey: "users.role.viewer", value: "viewer" },
];

const STATUS_OPTIONS = [
  { labelKey: "users.status.active", value: "active" },
  { labelKey: "users.status.suspended", value: "suspended" },
  { labelKey: "users.status.invited", value: "invited" },
];
```

组件内替换:

| 原文 | 替换为 |
| --- | --- |
| `title={editingUser ? "编辑用户" : "添加用户"}` | `title={editingUser ? t("users.form.editTitle") : t("users.form.addTitle")}` |
| `label="用户名"` | `label={t("users.columns.username")}` |
| `label="姓名"` | `label={t("users.columns.name")}` |
| `label="邮箱"` | `label={t("users.columns.email")}` |
| `label="手机号码"` | `label={t("users.columns.phone")}` |
| `label="角色"` | `label={t("users.columns.role")}` |
| `label="状态"` | `label={t("users.columns.status")}` |
| `label="取消"` | `label={t("common.cancel")}` |
| `label={editingUser ? "保存" : "创建"}` | `label={editingUser ? t("common.save") : t("common.create")}` |

- [ ] **Step 3: 验证 + Commit**

```bash
pnpm test && pnpm lint
git add src/features/users
git commit -m "feat: 用户管理文案接入 i18n"
```

---

### Task 9: 应用集成文案迁移

**Files:**
- Modify: `src/routes/_auth/apps.tsx`
- Modify: `src/features/apps/AppsGrid.tsx`
- 不改: `src/features/apps/types.ts`(`AppCategory` 的中文字面量是 mock 业务数据的值,保持不动)

**Interfaces:**
- Consumes: Task 1 的 `apps.*`、`common.actionFailed` key
- 关键设计:分类**值**是业务数据(中文字符串),**显示**经 i18n 映射翻译;筛选 sentinel 从 `"全部"` 改为 `"all"`。

- [ ] **Step 1: 迁移 apps 路由页**

`src/routes/_auth/apps.tsx`:`<Text type="display-3">应用集成</Text>` → `{t("apps.title")}`。

- [ ] **Step 2: 迁移 AppsGrid**

分类显示映射(模块级):

```tsx
import type { AppCategory } from "./types";

const CATEGORY_LABEL_KEYS: Record<AppCategory, string> = {
  "沟通协作": "apps.categories.collaboration",
  "开发工具": "apps.categories.devtools",
  "支付": "apps.categories.payment",
  "自动化": "apps.categories.automation",
  "数据分析": "apps.categories.analytics",
};

const CATEGORIES: AppCategory[] = ["沟通协作", "开发工具", "支付", "自动化", "数据分析"];
```

组件内筛选 state 与渲染改造:

```tsx
const [category, setCategory] = useState<AppCategory | "all">("all");

const visibleApps = useMemo(
  () => (category === "all" ? apps : apps.filter((app) => app.category === category)),
  [apps, category],
);
```

SegmentedControl 渲染(保持现有组件结构,items 为 "all" + CATEGORIES):

```tsx
<SegmentedControl
  label={t("apps.filterLabel")}
  value={category}
  onChange={(value) => setCategory(value as AppCategory | "all")}
>
  <SegmentedControlItem value="all" label={t("apps.categories.all")} />
  {CATEGORIES.map((item) => (
    <SegmentedControlItem key={item} value={item} label={t(CATEGORY_LABEL_KEYS[item])} />
  ))}
</SegmentedControl>
```

(实际 JSX 结构以文件为准 —— 若现在是 map 单数组渲染,改成上面的 sentinel + map 形式;卡片上如展示 `app.category`,同样包 `t(CATEGORY_LABEL_KEYS[app.category])`。)

其余替换:

| 原文 | 替换为 |
| --- | --- |
| `toast({ body: "已断开连接" })` | `toast({ body: t("apps.disconnected") })` |
| `toast({ body: "已连接" })` | `toast({ body: t("apps.connected") })` |
| `"操作失败，请稍后重试"`(toast body fallback) | `t("common.actionFailed")` |
| `title="没有匹配的应用" description="切换分类查看其他应用"` | `title={t("apps.emptyTitle")} description={t("apps.emptyDescription")}` |

- [ ] **Step 3: 验证 + Commit**

```bash
pnpm test && pnpm lint
git add src/routes/_auth/apps.tsx src/features/apps
git commit -m "feat: 应用集成文案接入 i18n(分类值保持业务数据、显示层翻译)"
```

---

### Task 10: 设置页 + http 错误文案迁移与最终验证

**Files:**
- Modify: `src/routes/_auth/settings/appearance.tsx`
- Modify: `src/routes/_auth/settings/profile.tsx`
- Modify: `src/features/settings/ProfileCard.tsx`
- Modify: `src/lib/http.ts`

**Interfaces:**
- Consumes: Task 1 的 `settings.*`、`userMenu.logout`、`errors.requestFailed` key
- 注意:`http.ts` 非组件上下文,用 `i18n.t`(import i18n from "i18next"),运行时求值。

- [ ] **Step 1: 迁移两个 settings 路由页**

- `appearance.tsx`:`<Text type="display-3">外观设置</Text>` → `{t("settings.appearance.pageTitle")}`
- `profile.tsx`:`<Text type="display-3">个人资料</Text>` → `{t("settings.profile.pageTitle")}`

- [ ] **Step 2: 迁移 ProfileCard**

| 原文 | 替换为 |
| --- | --- |
| `user?.name ?? "未登录"` | `user?.name ?? t("settings.profile.notLoggedIn")` |
| `label="退出登录"` | `label={t("userMenu.logout")}` |

- [ ] **Step 3: 迁移 http.ts 错误文案**

```ts
import i18n from "i18next";
```

beforeError 中:

```ts
return new ApiError(
  status,
  body.code ?? "UNKNOWN",
  body.message ?? i18n.t("errors.requestFailed", { status }),
);
```

(`http.test.ts` 不断言该 fallback 文案,无需改;测试 setup 已初始化 i18n 为 zh,`i18n.t` 可正常求值。)

- [ ] **Step 4: 全量硬编码中文扫描**

```bash
grep -rn --include="*.tsx" --include="*.ts" -P '[\x{4e00}-\x{9fff}]' src --exclude-dir=mocks --exclude-dir=i18n | grep -v '\.test\.' | grep -v '^\S*:\d*:\s*//' | grep -v '"沟通协作"\|"开发工具"\|"支付"\|"自动化"\|"数据分析"'
```

预期:剩余命中只有代码注释、`types.ts` 的 AppCategory 字面量、`LanguageControl` 的 "中文" label。发现遗漏 UI 文案则按前述模式补迁移(zh/en 同时补 key)。

- [ ] **Step 5: 最终验证**

```bash
pnpm test && pnpm lint && pnpm build
```

预期:全部通过。然后 `pnpm dev` 手动走查(curl 需 `--noproxy '*'`,浏览器不受影响):

1. 登录页切 English → 表单变英文,登录后全站英文
2. 顶栏切中文 → 即时生效,刷新后保持(localStorage `astryx-admin-ui`)
3. 设置→外观页两个控件与顶栏状态一致
4. `document.documentElement.lang` 随语言为 `zh-CN`/`en`

- [ ] **Step 6: Commit**

```bash
git add src/routes/_auth/settings src/features/settings/ProfileCard.tsx src/lib/http.ts
git commit -m "feat: 设置页与 http 错误文案接入 i18n"
```
